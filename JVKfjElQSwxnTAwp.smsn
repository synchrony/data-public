@id JVKfjElQSwxnTAwp
@title RenameMe
@created 1427898782351
@text ```
bp <- barplot(means)
axis(1, at=bp, labels=steps)

# only some ticks
lab <- c("A","B","C")
axis(1, at=bp[c(1,5,10)],labels=lab)
```
