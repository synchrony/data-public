* :axpxMD3: heapsort is a variant of selection sort which uses a heap to store "smallest items" instead of repetitively searching through the remaining part of the list
    @created 1381088977916
    @sharability 0.75
    @weight 0.5
    * :Jf2LEdz: 
