@id 9M4tcAeFtxOdTrLt
@title SWI-Prolog's CLP(FD) is not clever about big domains
@created 1515290791159
@priority 0.5
@text ```
I ran this and it hadn't answered two minutes later. Probably it's building the entire list.

. use_module(library(clpfd)).
- X #< 999999999, X #>= 0, labeling([max(X)],[X]).
```
