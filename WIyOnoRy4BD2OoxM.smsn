@id WIyOnoRy4BD2OoxM
@title an alternative data model for database systems.  In the associative model, everything which has “discrete independent existence” is modeled as an entity, and relationships between them are modeled as associations.\u201Cdiscrete independent existence\u201D is modeled as an entity, and relationships between them are modeled as associations.
@created 1367996761028
