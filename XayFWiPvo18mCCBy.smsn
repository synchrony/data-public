@id XayFWiPvo18mCCBy
@title subset a frame's rows based on conditions
@created 1520964871789
@text ```
# works generally
  df[ (df["money"] > 1) & (df["time"] < 9)] 
  df[ df["time"].isnull() ]

# works if "money" and "time" are boolean variables
  df[ money & time ]
```
