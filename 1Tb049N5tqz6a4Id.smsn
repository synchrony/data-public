@id 1Tb049N5tqz6a4Id
@title using it to program an exception
@created 1513207625178
@text ```
Vincent enjoys all burgers that are not big-kahuna.

  enjoys(vincent,X)  :-  big_kahuna_burger(X),!,fail.
  enjoys(vincent,X)  :-  burger(X).

The ! means that upon reaching the fail, we won't try the second branch.
```
