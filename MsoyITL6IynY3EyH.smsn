@id MsoyITL6IynY3EyH
@title join (unlike-type) columns
@created 1496703508467
@text ```
x = pd.DataFrame(
    { 'a': np.random.normal(size=10)
    , 'c': np.array( range(10) ) * 2 + 5
    }
    , index = np.array( range(10) )
)
```
